using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PrimeiroScriptCsharp : MonoBehaviour
{
    // Exemplos de Variáveis Básicas C#
    char Sexo = 'M';
    int Idade = 18;
    float Altura = 1.75f;
    double Saldo = 500.0;
    bool Adulto = false;

    // Exemplo de Variáveis Unity
    GameObject Mochila;
    Rigidbody ComponenteDeFisica;
    Collider ComponenteDeColisao;

    // Start é chamado um vez ao iniciar o jogo
    void Start()
    {
       

    }

    // Update é chamado a cada frame do jogo
    void Update()
    {

    }
}
